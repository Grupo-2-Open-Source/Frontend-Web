import { NotificationsOwner } from './notifications-owner';

describe('NotificationsOwner', () => {
  it('should create an instance', () => {
    expect(new NotificationsOwner()).toBeTruthy();
  });
});
